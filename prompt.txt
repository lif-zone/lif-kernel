Session log
===========

---

USER: in ~/lif-kernel/util.js there is rpc_base that implements a generic JSON RPC protocol. I would like to extend it with an additional optional field 'seq', which creates a long-term 'connection': the 'id' will be used long term, until the 'seq' is closed.

Wire format:
  Open:    { seq: "0 open", id: 234, method: "long_request", params: {...} }
  Reply:   { seq: "0",      id: 234, result: {...} }
  Client:  { seq: "1",      id: 234, method: "next_step", params: {...} }
  Reply:   { seq: "1",      id: 234, result: ... }
  Server:  { seq: "r 0",   id: 234, method: "server_push", params: {...} }
  Reply:   { seq: "r 0",   id: 234, result: ... }
  Close:   { seq: "close", id: 234 }

CLAUDE: Added sequence RPC support directly to rpc_base in util.js:
- seqs = {} field — active sequences keyed by id
- _seq_create(id) — builds seq state + api: {call, push, on, close}
- seq_open(method, params) — client opens sequence, returns {result, seq}
- on_seq_open(msg) — server handles "N open", calls handler with (params, seq_api)
- on_seq_msg(msg) — dispatches all seq messages (open, close, calls, responses)
- seq_method(method, fn) — registers server-side handler; fn receives (params, seq_api)
- close() — extended to throw on all pending seq waits

Routing in on_msg: if msg.seq != null, routed to on_seq_msg before normal dispatch.

seq_api methods:
  call(method, params)  — client sends seq "N", awaits response
  push(method, params)  — server sends seq "r N", awaits response
  on(method, fn)        — register handler for incoming calls within this sequence
  close()               — sends {seq: "close", id}, cleans up state

Usage:
  // Server
  rpc.seq_method('stream', async(params, seq)=>{
    seq.on('next', async(p, seq)=>{ return 'ok'; });
    const r = await seq.push('notify', {data: 1});
    return 'started';
  });

  // Client
  const {result, seq} = await rpc.seq_open('stream', {x: 1});
  seq.on('notify', async(p)=>{ return 'ack'; });
  const r = await seq.call('next', {step: 1});
  await seq.close();

---

---
USER:
I want lif-kernel/trunk.js (and required other files) to expand the current code from star topology (1 backbone) to DHT mesh backbone.

CLAUDE:
Analyzed the existing star topology in trunk.js and lifnet.js. Implemented mesh backbone in trunk.js with:
- peer_conn (trunk_id→rpc) and rg_peer (rg_id→trunk_id) module-level maps
- rg_id registration/disconnect gossips peer_rg_add/peer_rg_remove to all peers
- rcall forwards to peer trunk via peer_rcall when rg_id not local
- rconnect creates cross-trunk pipe via peer_rconnect (C1 ↔ trunk A ↔ trunk B ↔ C2)
- peer_hello exchanges state on connect
- trunk_peer_connect(url) new export to join the mesh from server.js
- Added OV to imports; single-trunk deployments unchanged

---
USER:
lifnet listen + connect across two trunks failed. Also made trunk_peer_add sync with retry like lifnet.js trunk_add.

CLAUDE:
Fixed two issues:
1. trunk_peer_connect → trunk_peer_add(url): sync, fires _peer_connect in background with retry (PEER_RETRY_MS=5s), same pattern as lifnet.js trunk_connect. Added peer_introduce gossip for self-forming mesh.
2. topic_get only checked local topics. Added peer_topics map (topic→{rg_id:trunk_id}), gossip via peer_topic_add/remove on topic_pub/unpub, peer_hello now exchanges topic snapshots. topic_get merges local+remote. Peer disconnect cleans peer_topics.
Added --peer flag to server_lib.js for CLI seeding.

---
USER:
implement the rpc_sock fix: use [{c: id}, seq] array ids instead of separate id/seq fields, with c/s tagging to prevent id collisions between sides, enabling nested rpc_sock

CLAUDE:
Implemented the rpc_sock protocol fix in util.js:
- rpc_base.emit_msg: handle array ids by routing on JSON.stringify(msg.id[0]) key
- rpc_sock.send: build wire id by prepending this._id — null→[...this._id], array→[...this._id,...inner], number→[...this._id, N]
- rpc_sock.emit_close (new override): deregisters from parent via rpc.on_id(JSON.stringify(this._id[0])) before calling super
- rpc_sock.set_events: registers with JSON.stringify key; strips first element; rest=[]→notify/close, rest=[N]→leaf seq, rest=[{},...]→nested array
- rpc_sock.connect: this._id = [{c: this.rpc.id_get()}]
- rpc_sock.accept: this._id = [msg.id[0]]
- rpc_sock.listen: validates Array.isArray(msg.id) && length>=2, extracts seq = msg.id[last]
- rpc_sock.close: sends {close: true} as notify (no seq), relies on emit_close for deregistration

---
USER:
dns_s.js getting 'unsupported type 257' (CAA). add CAA support to answer letsencrypt queries.

CLAUDE:
Added to lif-kernel/dns_s.js:
- TYPE_CAA=257 const (dns2 doesn't define Packet.TYPE.CAA)
- res_type_caa(name): hand-encodes CAA RDATA per RFC 6844 ([flags:1][tag_len:1][tag:N][value]) using dns2's raw resource.data fallback for unknown types. Reads E.caa_issuers (defaults to ['letsencrypt.org']).
- Added case TYPE_CAA in switch
- Added res_type_caa to res_type_any

---
USER:
I want to be able to run lif-kernel from any package with 'npx lif-kernel serve -p 1234'. what do I need to add?

CLAUDE:
Added bin/lif-kernel.js entry point and wired it in package.json:
- bin/lif-kernel.js: parses 'serve' command, strips it from argv, imports web/serve.js
- package.json: added "bin": {"lif-kernel": "./bin/lif-kernel.js"} and "bin/lif-kernel.js" to files array

---
USER:
add in-process fast path for same-process lifnet<->trunk routing to avoid TCP loopback

CLAUDE:
Added Trunk_local to lifnet.js and trunk_local() to trunk.js:
- trunk.js: added sock_pair to imports; trunk_local() creates sock_pair, calls rpc_methods_basic+rpc_methods_lifnet_trunk on server end, returns client end
- lifnet.js: Trunk_local extends Trunk_remote — constructor takes pre-built rpc, start() calls rg_id/topic_pub/method registration then emits 'online'. Added lifnet.trunk_add_local(rpc) and exported lifnet_init_local(rpc) which sets lifnet_inited=true and calls trunk_add_local
- server_lib.js: imports trunk_local and lifnet_init_local; calls lifnet_init_local(trunk_local()) in run() when lifnet_trunk enabled, before start_web()

---
USER:
refactor: extract Trunk base class with shared methods, Trunk_remote extends Trunk (WebSocket reconnect logic), Trunk_local extends Trunk (just start()). rename old Trunk to Trunk_remote.

CLAUDE:
Refactored lifnet.js:
- Trunk (base): emit_status, _rpc_method_set, _rpc_method_del, trunk_call, topic_pub, topic_unpub, topic_get, close
- Trunk_remote extends Trunk: adds url, last, et_connect, overrides emit_status (logs url, calls super), start/_connect_single/_connect_loop
- Trunk_local extends Trunk: constructor sets this.rpc, start() does rg_id+topics+methods+emit_status('online')
- _ws_trunks() now uses instanceof Trunk (catches both subclasses)
- Fixed Trunk_remote.emit_status to call super.emit_status instead of duplicating body

---
USER:
rename Trunk_remote→Trunk_uplink, Trunk_local→Trunk_router, and all related functions

CLAUDE:
Renamed across lifnet.js, trunk.js, server_lib.js:
- Trunk_remote → Trunk_uplink
- Trunk_local → Trunk_router
- trunk_local() → trunk_router()
- lifnet_init_local() → lifnet_init_router()
- trunk_add_local() → trunk_add_router()

---
USER:
rename Trunk_uplink/Trunk_router → Trunk_uplink/Trunk_route, then Trunk_route → Trunk_router, then rename trunk_add() → trunk_uplink_add()

CLAUDE:
Final naming across lifnet.js, trunk.js, server_lib.js:
- Trunk_uplink (leaf default gateway, connects via WebSocket to remote trunk)
- Trunk_router (in-process connection to local trunk routing table)
- trunk_router(), lifnet_init_router(), trunk_add_router()
- trunk_add(url) → trunk_uplink_add(url)

---
USER:
when a Trunk_router connects to itself (same rg_id) disconnect gracefully. also clean up stale XXX comments in rpc_sock_rconnect

CLAUDE:
- trunk.js _peer_connect: after peer_hello, if ret.trunk_id==g_rg_id → console.log('disconnecting from self') + rpc.close() + return (no retry)
- trunk.js rpc_sock_rconnect: removed stale XXX comments from loopback/localhost throws

---
USER:
decouple Trunk retry logic from lifnet. lifnet should be 'above' and control trunks via set_aggressive(bool): aggressive=1s retry, rest=60s retry. trunks should not know about lifnet status.

CLAUDE:
- Added RETRY_REST_MS=60000 to lifnet.js
- Trunk_uplink: added retry_ms=RETRY_MS, _sleep_et fields; _connect_loop uses this.retry_ms and tracks sleep in _sleep_et; added set_aggressive(v) which sets retry_ms and cancels _sleep_et if switching to aggressive
- Removed the previous lifnet-status-aware once() hack from _connect_loop
- Lifnet._status_update: when status changes, calls set_aggressive?.(aggressive) on all non-online trunks — aggressive when offline, rest when online

---
USER:
rename Trunk_remote→Trunk_uplink, Trunk_local→Trunk_router, and all related functions

CLAUDE:
Renamed across lifnet.js, trunk.js, server_lib.js:
- Trunk_remote → Trunk_uplink
- Trunk_local → Trunk_router
- trunk_local() → trunk_router()
- lifnet_init_local() → lifnet_init_router()
- trunk_add_local() → trunk_add_router()
---
USER:
in lif-kernel/kernel.js add support for parse lif: {transform: {'./**/*.js': {ext: '.jsx'}}} - this tells it that for transformational purposes, to handle this file as-if it has the jsx extension in its name.

CLAUDE:
Added two helper functions and modified file_tsx_to_js in kernel.js:
- glob_match(pattern, path): converts glob to regex (handles **/ and *)
- pkg_transform_ext(pkg, lmod, pkg_lmod): looks up lif.transform in package.json, computes relative path, matches patterns, returns override ext
- file_tsx_to_js: calls pkg_transform_ext to override type before Babel transform decision
